@model IEnumerable<Entities.Models.HsOwner>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.eMail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WorkOrderId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.eMail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkOrderId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HsOwnerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HsOwnerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HsOwnerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
